---
apiVersion: batch/v1
kind: Job
metadata:
  name: ${job}
  namespace: ${ns}
spec:
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      containers:
        - name: main
          image: docker.io/restic/restic:0.16.4
          args: ["snapshots"]
          envFrom:
            - secretRef:
                #name: ${app}-volsync-secret
                name: ${app}-restic-config
          resources: {}
          env:
            - name: RESTIC_CACERT
              value: /run/secrets/custom_ca
          volumeMounts:
            - name: custom-ca
              subPath: custom_ca
              mountPath: /run/secrets/custom_ca
      volumes:
        - name: custom-ca
          secret:
            secretName: "${app}-restic-config"
            items:
              - key: CUSTOM_CA_CRT
                path: custom_ca