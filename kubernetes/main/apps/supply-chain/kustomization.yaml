---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: supply-chain

resources:
  - ./namespace.yaml
  # Flux Kustomizations
  - ./policy-controller/ks.yaml
