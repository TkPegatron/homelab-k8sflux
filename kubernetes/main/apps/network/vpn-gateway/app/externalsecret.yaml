apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name pod-gateway-vpn
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: openbao-store
    kind: ClusterSecretStore
  target:
    name: *name
  dataFrom:
    - extract:
        key: *name